server:
  port: 8080

spring:
  application:
    name: parkvision-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

parkvision:
  persistence:
    mode: fallback
  ai:
    inference-mode: fallback
