services:
  backend:
    build:
      context: ../backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: demo

  frontend:
    build:
      context: ../frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
